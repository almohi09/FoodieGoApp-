apiVersion: v1
kind: Service
metadata:
  name: foodiego-backend
  namespace: foodiego
spec:
  selector:
    app: foodiego-backend
  ports:
    - port: 80
      targetPort: 4000
      protocol: TCP
      name: http
